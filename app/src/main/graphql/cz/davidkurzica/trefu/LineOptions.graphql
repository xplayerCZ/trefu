query LineOptions($stopId: ID!) {
    stop(id: $stopId) {
        routeStops {
            route {
                line {
                    id
                    shortCode
                }
            }
        }
    }
}