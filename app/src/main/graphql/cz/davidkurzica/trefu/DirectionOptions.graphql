query DirectionOptions($lineId: ID!) {
    line(id: $lineId) {
        routes {
            lastRouteStop {
                stop {
                    name
                }
            }
        }
    }
}